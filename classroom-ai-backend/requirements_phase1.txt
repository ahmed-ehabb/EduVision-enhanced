# ================================
# PHASE 1: MODEL TESTING REQUIREMENTS
# Optimized for RTX 3050 GPU Testing
# ================================

# Core API Framework (for future use)
fastapi==0.115.12
uvicorn==0.34.2
python-multipart==0.0.20
pydantic==2.10.5

# Database (for future use)
sqlalchemy==2.0.32
psycopg2-binary==2.9.9

# Core ML & Deep Learning (GPU Support - CUDA 12.1)
--index-url https://download.pytorch.org/whl/cu121
torch==2.5.0
torchvision==0.20.0
torchaudio==2.5.0

# HuggingFace Transformers
transformers==4.38.2
huggingface-hub==0.31.1
accelerate==0.28.0

# Model Optimization (CRITICAL for RTX 3050)
bitsandbytes==0.45.5
optimum==1.22.0

# Scientific Computing
numpy==1.24.3
pandas==2.3.0
scipy==1.15.2
scikit-learn==1.4.1.post1

# Audio Processing (for ASR testing)
soundfile==0.13.1
librosa==0.10.1

# NLP (for preprocessing)
nltk==3.9.1
langdetect==1.0.9

# Utilities
python-dotenv==1.0.1
tqdm==4.66.2
requests==2.31.0

# Monitoring (CRITICAL for GPU memory tracking)
psutil==6.1.0
pynvml==11.5.0

# Testing
pytest==8.3.4

# Note: TensorFlow, face recognition, and other heavy packages
# are NOT needed for Phase 1 model testing
# Install those later when needed
